const appName = '/navigator';

const urls = {
  login: {
    auth: `${appName}/auth`,
  },
  links: {
    getAll: `${appName}/links`,
  },
  tabs: {
    getAll: `${appName}/tabs`,
  },
};

export default urls;
